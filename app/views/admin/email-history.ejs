<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> | CrystosJewel Admin</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        :root {
            /* Couleurs principales de votre site */
            --rose-gold: #b76e79;
            --rose-gold-light: #e8c2c8;
            --rose-gold-dark: #7d4b53;
            --rose-gold-hover: #c98490;
            --cream: #fff8f0;
            --dark-text: #3a3a3a;
            --light-text: #f5f5f5;
            
            /* Couleurs système */
            --success: #28a745;
            --warning: #ffc107;
            --danger: #dc3545;
            --info: #17a2b8;
            --dark: var(--dark-text);
            --gray-light: #f9f9f9;
            --gray-medium: #e0e0e0;
            --white: #ffffff;
            
            /* Effets */
            --border-radius: 12px;
            --shadow: 0 4px 12px rgba(183, 110, 121, 0.1);
            --shadow-lg: 0 6px 20px rgba(183, 110, 121, 0.15);
            --transition: all 0.3s ease;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Montserrat', -apple-system, BlinkMacSystemFont, sans-serif;
            background: linear-gradient(135deg, var(--cream) 0%, var(--rose-gold-light) 100%);
            color: var(--dark);
            line-height: 1.6;
            min-height: 100vh;
        }

        .container {
            max-width: 1400px;
            margin: 0 auto;
            padding: 20px;
        }

        .header {
            background: var(--white);
            border-radius: var(--border-radius);
            padding: 30px;
            margin-bottom: 30px;
            box-shadow: var(--shadow);
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .header-content h1 {
            font-size: 2rem;
            font-weight: 700;
            color: var(--rose-gold);
            margin-bottom: 10px;
        }

        .breadcrumb {
            color: var(--gray-medium);
            font-size: 0.9rem;
        }

        .breadcrumb a {
            color: var(--rose-gold);
            text-decoration: none;
        }

        .header-actions {
            display: flex;
            gap: 15px;
        }

        .btn {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            padding: 12px 24px;
            border: none;
            border-radius: 8px;
            font-size: 14px;
            font-weight: 600;
            cursor: pointer;
            transition: var(--transition);
            text-decoration: none;
        }

        .btn-primary {
            background: linear-gradient(135deg, var(--rose-gold) 0%, var(--rose-gold-dark) 100%);
            color: var(--white);
        }

        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: var(--shadow-lg);
        }

        .btn-secondary {
            background: var(--gray-light);
            color: var(--dark);
            border: 2px solid #e5e7eb;
        }

        .btn-secondary:hover {
            background: #f1f5f9;
            border-color: var(--rose-gold);
        }

        /* Stats Cards */
        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 25px;
            margin-bottom: 40px;
        }

        .stat-card {
            background: var(--white);
            border-radius: var(--border-radius);
            padding: 25px;
            box-shadow: var(--shadow);
            text-align: center;
            transition: var(--transition);
        }

        .stat-card:hover {
            transform: translateY(-5px);
            box-shadow: var(--shadow-lg);
        }

        .stat-icon {
            width: 60px;
            height: 60px;
            background: linear-gradient(135deg, var(--rose-gold) 0%, var(--rose-gold-dark) 100%);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 15px;
            color: var(--white);
            font-size: 1.5rem;
        }

        .stat-value {
            font-size: 2rem;
            font-weight: 700;
            color: var(--rose-gold);
            margin-bottom: 8px;
        }

        .stat-label {
            color: var(--gray-medium);
            font-size: 0.9rem;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        /* Filters */
        .filters-section {
            background: var(--white);
            border-radius: var(--border-radius);
            padding: 25px;
            margin-bottom: 30px;
            box-shadow: var(--shadow);
        }

        .filters-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 20px;
            align-items: end;
        }

        .filter-group {
            display: flex;
            flex-direction: column;
            gap: 8px;
        }

        .filter-label {
            font-weight: 600;
            color: var(--dark);
            font-size: 0.9rem;
        }

        .filter-control {
            padding: 10px 15px;
            border: 2px solid #e5e7eb;
            border-radius: 8px;
            font-size: 14px;
            transition: var(--transition);
        }

        .filter-control:focus {
            outline: none;
            border-color: var(--rose-gold);
            box-shadow: 0 0 0 3px rgba(183, 110, 121, 0.1);
        }

        /* Campaigns Grid */
        .campaigns-section {
            background: var(--white);
            border-radius: var(--border-radius);
            padding: 30px;
            box-shadow: var(--shadow);
        }

        .campaigns-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 25px;
            padding-bottom: 15px;
            border-bottom: 2px solid var(--gray-light);
        }

        .campaigns-title {
            font-size: 1.3rem;
            font-weight: 600;
            color: var(--dark);
        }

        .campaigns-grid {
            display: grid;
            gap: 20px;
        }

        .campaign-card {
            background: var(--gray-light);
            border-radius: 12px;
            padding: 25px;
            border: 2px solid transparent;
            transition: var(--transition);
            position: relative;
        }

        .campaign-card:hover {
            border-color: var(--rose-gold);
            transform: translateY(-2px);
            box-shadow: var(--shadow);
        }

        .campaign-header {
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
            margin-bottom: 15px;
        }

        .campaign-name {
            font-size: 1.1rem;
            font-weight: 600;
            color: var(--dark);
            margin-bottom: 5px;
        }

        .campaign-date {
            font-size: 0.85rem;
            color: var(--gray-medium);
        }

        .campaign-status {
            padding: 6px 12px;
            border-radius: 20px;
            font-size: 0.8rem;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .status-sent {
            background: #d4edda;
            color: #155724;
        }

        .status-failed {
            background: #f8d7da;
            color: #721c24;
        }

        .status-pending {
            background: #fff3cd;
            color: #856404;
        }

        .campaign-stats {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(80px, 1fr));
            gap: 15px;
            margin-top: 15px;
        }

        .stat-item {
            text-align: center;
        }

        .stat-number {
            font-size: 1.2rem;
            font-weight: 700;
            color: var(--rose-gold);
        }

        .stat-text {
            font-size: 0.8rem;
            color: var(--gray-medium);
            text-transform: uppercase;
        }

        .campaign-actions {
            position: absolute;
            top: 15px;
            right: 15px;
            opacity: 0;
            transition: var(--transition);
        }

        .campaign-card:hover .campaign-actions {
            opacity: 1;
        }

        .action-btn {
            background: var(--white);
            border: 1px solid var(--gray-medium);
            width: 35px;
            height: 35px;
            border-radius: 50%;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            transition: var(--transition);
            margin-left: 5px;
        }

        .action-btn:hover {
            background: var(--rose-gold);
            color: var(--white);
            border-color: var(--rose-gold);
        }

        /* Empty State */
        .empty-state {
            text-align: center;
            padding: 60px 20px;
            color: var(--gray-medium);
        }

        .empty-icon {
            font-size: 4rem;
            margin-bottom: 20px;
            color: var(--rose-gold-light);
        }

        .empty-title {
            font-size: 1.3rem;
            font-weight: 600;
            margin-bottom: 10px;
            color: var(--dark);
        }

        .empty-message {
            font-size: 1rem;
            margin-bottom: 25px;
        }

        /* Success Rate Bar */
        .success-rate-bar {
            width: 100%;
            height: 8px;
            background: #e5e7eb;
            border-radius: 4px;
            overflow: hidden;
            margin-top: 10px;
        }

        .success-rate-fill {
            height: 100%;
            background: linear-gradient(90deg, var(--rose-gold) 0%, var(--success) 100%);
            transition: width 0.3s ease;
        }

        /* Responsive */
        @media (max-width: 1200px) {
            .header {
                flex-direction: column;
                gap: 20px;
                text-align: center;
            }

            .header-actions {
                width: 100%;
                justify-content: center;
            }
        }

        @media (max-width: 768px) {
            .container {
                padding: 10px;
            }

            .header {
                padding: 20px;
            }

            .header h1 {
                font-size: 1.5rem;
            }

            .stats-grid {
                grid-template-columns: repeat(2, 1fr);
                gap: 15px;
            }

            .filters-grid {
                grid-template-columns: 1fr;
            }

            .campaigns-section {
                padding: 20px;
            }

            .campaign-card {
                padding: 20px;
            }

            .campaign-stats {
                grid-template-columns: repeat(2, 1fr);
            }
        }

        @media (max-width: 480px) {
            .stats-grid {
                grid-template-columns: 1fr;
            }

            .stat-card {
                padding: 20px;
            }

            .campaign-actions {
                position: relative;
                top: auto;
                right: auto;
                opacity: 1;
                margin-top: 15px;
                text-align: center;
            }
        }
    </style>
</head>

<body>
    <!-- Navigation -->
    <%- include('../partials/navbarre.ejs') %>

    <div class="container">
        <!-- Header -->
        <div class="header">
            <div class="header-content">
                <h1>
                    <i class="fas fa-history"></i>
                    <%= title %>
                </h1>
                <div class="breadcrumb">
                    <a href="/admin">Administration</a> › 
                    <a href="/admin/emails">Emails</a> › 
                    Historique
                </div>
            </div>
            <div class="header-actions">
                <a href="/admin/email-editor" class="btn btn-primary">
                    <i class="fas fa-plus"></i>
                    Nouvelle campagne
                </a>
                <button class="btn btn-secondary" onclick="exportHistory()">
                    <i class="fas fa-download"></i>
                    Exporter
                </button>
            </div>
        </div>

        <!-- Statistiques -->
        <div class="stats-grid">
            <div class="stat-card">
                <div class="stat-icon">
                    <i class="fas fa-envelope"></i>
                </div>
                <div class="stat-value"><%= stats.total || 0 %></div>
                <div class="stat-label">Campagnes totales</div>
            </div>
            <div class="stat-card">
                <div class="stat-icon">
                    <i class="fas fa-paper-plane"></i>
                </div>
                <div class="stat-value"><%= stats.sent || 0 %></div>
                <div class="stat-label">Emails envoyés</div>
            </div>
            <div class="stat-card">
                <div class="stat-icon">
                    <i class="fas fa-exclamation-triangle"></i>
                </div>
                <div class="stat-value"><%= stats.failed || 0 %></div>
                <div class="stat-label">Échecs</div>
            </div>
            <div class="stat-card">
                <div class="stat-icon">
                    <i class="fas fa-chart-line"></i>
                </div>
                <div class="stat-value"><%= stats.successRate || 0 %>%</div>
                <div class="stat-label">Taux de succès</div>
            </div>
        </div>

        <!-- Filtres -->
        <div class="filters-section">
            <div class="filters-grid">
                <div class="filter-group">
                    <label class="filter-label">Rechercher une campagne</label>
                    <input type="text" id="searchInput" class="filter-control" placeholder="Nom de la campagne...">
                </div>
                <div class="filter-group">
                    <label class="filter-label">Statut</label>
                    <select id="statusFilter" class="filter-control">
                        <option value="">Tous les statuts</option>
                        <option value="sent">Envoyé</option>
                        <option value="failed">Échec</option>
                        <option value="pending">En attente</option>
                    </select>
                </div>
                <div class="filter-group">
                    <label class="filter-label">Type</label>
                    <select id="typeFilter" class="filter-control">
                        <option value="">Tous les types</option>
                        <option value="campaign">Campagne</option>
                        <option value="promotional">Promotionnel</option>
                        <option value="newsletter">Newsletter</option>
                    </select>
                </div>
                <div class="filter-group">
                    <label class="filter-label">Période</label>
                    <select id="periodFilter" class="filter-control">
                        <option value="">Toute période</option>
                        <option value="today">Aujourd'hui</option>
                        <option value="week">Cette semaine</option>
                        <option value="month">Ce mois</option>
                        <option value="quarter">Ce trimestre</option>
                    </select>
                </div>
            </div>
        </div>

        <!-- Liste des campagnes -->
        <div class="campaigns-section">
            <div class="campaigns-header">
                <div class="campaigns-title">
                    <i class="fas fa-list"></i>
                    Campagnes récentes
                </div>
                <div>
                    <span id="campaignsCount"><%= campaigns ? campaigns.length : 0 %></span> campagne(s)
                </div>
            </div>

            <div class="campaigns-grid" id="campaignsGrid">
                <% if (campaigns && campaigns.length > 0) { %>
                    <% campaigns.forEach(campaign => { %>
                        <div class="campaign-card" data-status="<%= campaign.status %>" data-name="<%= campaign.name.toLowerCase() %>">
                            <div class="campaign-header">
                                <div>
                                    <div class="campaign-name"><%= campaign.name %></div>
                                    <div class="campaign-date">
                                        <%= new Date(campaign.created_at).toLocaleDateString('fr-FR', { 
                                            day: '2-digit', 
                                            month: '2-digit', 
                                            year: 'numeric',
                                            hour: '2-digit',
                                            minute: '2-digit'
                                        }) %>
                                    </div>
                                </div>
                                <div class="campaign-status status-<%= campaign.status %>">
                                    <%= campaign.status === 'sent' ? 'Envoyé' : 
                                        campaign.status === 'failed' ? 'Échec' : 'En attente' %>
                                </div>
                            </div>

                            <div class="campaign-stats">
                                <div class="stat-item">
                                    <div class="stat-number"><%= campaign.recipients || 0 %></div>
                                    <div class="stat-text">Destinataires</div>
                                </div>
                                <div class="stat-item">
                                    <div class="stat-number"><%= campaign.sent || 0 %></div>
                                    <div class="stat-text">Envoyés</div>
                                </div>
                                <div class="stat-item">
                                    <div class="stat-number"><%= campaign.failed || 0 %></div>
                                    <div class="stat-text">Échecs</div>
                                </div>
                                <div class="stat-item">
                                    <div class="stat-number"><%= campaign.success_rate || 0 %>%</div>
                                    <div class="stat-text">Succès</div>
                                </div>
                            </div>

                            <% if (campaign.success_rate !== undefined) { %>
                                <div class="success-rate-bar">
                                    <div class="success-rate-fill" style="width: <%= campaign.success_rate %>%"></div>
                                </div>
                            <% } %>

                            <div class="campaign-actions">
                                <button class="action-btn" onclick="previewCampaign('<%= campaign.id %>')" title="Aperçu">
                                    <i class="fas fa-eye"></i>
                                </button>
                                <button class="action-btn" onclick="duplicateCampaign('<%= campaign.id %>')" title="Dupliquer">
                                    <i class="fas fa-copy"></i>
                                </button>
                                <button class="action-btn" onclick="deleteCampaign('<%= campaign.id %>')" title="Supprimer">
                                    <i class="fas fa-trash"></i>
                                </button>
                            </div>
                        </div>
                    <% }); %>
                <% } else { %>
                    <div class="empty-state">
                        <div class="empty-icon">
                            <i class="fas fa-envelope-open"></i>
                        </div>
                        <div class="empty-title">Aucune campagne trouvée</div>
                        <div class="empty-message">
                            Commencez par créer votre première campagne email pour voir l'historique ici.
                        </div>
                        <a href="/admin/email-editor" class="btn btn-primary">
                            <i class="fas fa-plus"></i>
                            Créer une campagne
                        </a>
                    </div>
                <% } %>
            </div>
        </div>
    </div>

    <script>
        // ==========================================
        // 📧 HISTORIQUE DES EMAILS - JAVASCRIPT
        // ==========================================

        document.addEventListener('DOMContentLoaded', function() {
            console.log('📧 Initialisation historique emails');
            
            initializeFilters();
            initializeSearch();
        });

        // ==========================================
        // 🔍 GESTION DES FILTRES
        // ==========================================
        function initializeFilters() {
            const statusFilter = document.getElementById('statusFilter');
            const typeFilter = document.getElementById('typeFilter');
            const periodFilter = document.getElementById('periodFilter');

            statusFilter.addEventListener('change', applyFilters);
            typeFilter.addEventListener('change', applyFilters);
            periodFilter.addEventListener('change', applyFilters);
        }

        function initializeSearch() {
            const searchInput = document.getElementById('searchInput');
            let searchTimeout;

            searchInput.addEventListener('input', function() {
                clearTimeout(searchTimeout);
                searchTimeout = setTimeout(() => {
                    applyFilters();
                }, 300);
            });
        }

        function applyFilters() {
            const searchTerm = document.getElementById('searchInput').value.toLowerCase();
            const statusFilter = document.getElementById('statusFilter').value;
            const typeFilter = document.getElementById('typeFilter').value;
            const periodFilter = document.getElementById('periodFilter').value;

            const campaigns = document.querySelectorAll('.campaign-card');
            let visibleCount = 0;

            campaigns.forEach(campaign => {
                const name = campaign.dataset.name || '';
                const status = campaign.dataset.status || '';
                const type = campaign.dataset.type || '';
                
                let shouldShow = true;

                // Filtre de recherche
                if (searchTerm && !name.includes(searchTerm)) {
                    shouldShow = false;
                }

                // Filtre de statut
                if (statusFilter && status !== statusFilter) {
                    shouldShow = false;
                }

                // Filtre de type
                if (typeFilter && type !== typeFilter) {
                    shouldShow = false;
                }

                // Afficher/masquer la campagne
                if (shouldShow) {
                    campaign.style.display = 'block';
                    visibleCount++;
                } else {
                    campaign.style.display = 'none';
                }
            });

            // Mettre à jour le compteur
            document.getElementById('campaignsCount').textContent = visibleCount;
        }

        // ==========================================
        // 🎯 ACTIONS SUR LES CAMPAGNES
        // ==========================================
        async function previewCampaign(campaignId) {
            try {
                const response = await fetch(`/admin/emails/${campaignId}/preview`);
                const data = await response.json();

                if (data.success) {
                    // Afficher l'aperçu dans une modale ou nouvelle fenêtre
                    showCampaignPreview(data.campaign);
                } else {
                    alert('Erreur lors de la prévisualisation: ' + data.message);
                }
            } catch (error) {
                console.error('❌ Erreur prévisualisation:', error);
                alert('Erreur lors de la prévisualisation');
            }
        }

        async function duplicateCampaign(campaignId) {
            if (!confirm('Voulez-vous dupliquer cette campagne ?')) {
                return;
            }

            try {
                const response = await fetch(`/admin/emails/${campaignId}/duplicate`, {
                    method: 'POST'
                });
                const data = await response.json();

                if (data.success) {
                    alert('Campagne dupliquée avec succès');
                    // Rediriger vers l'éditeur avec le contenu dupliqué
                    window.location.href = '/admin/email-editor';
                } else {
                    alert('Erreur lors de la duplication: ' + data.message);
                }
            } catch (error) {
                console.error('❌ Erreur duplication:', error);
                alert('Erreur lors de la duplication');
            }
        }

        async function deleteCampaign(campaignId) {
            if (!confirm('Êtes-vous sûr de vouloir supprimer cette campagne ? Cette action est irréversible.')) {
                return;
            }

            try {
                const response = await fetch(`/admin/emails/${campaignId}`, {
                    method: 'DELETE'
                });
                const data = await response.json();

                if (data.success) {
                    // Supprimer la carte de la vue
                    const campaignCard = document.querySelector(`[data-campaign-id="${campaignId}"]`);
                    if (campaignCard) {
                        campaignCard.remove();
                    }
                    
                    alert('Campagne supprimée avec succès');
                    location.reload(); // Recharger pour mettre à jour les stats
                } else {
                    alert('Erreur lors de la suppression: ' + data.message);
                }
            } catch (error) {
                console.error('❌ Erreur suppression:', error);
                alert('Erreur lors de la suppression');
            }
        }

        function showCampaignPreview(campaign) {
            // Créer une fenêtre modale ou popup pour l'aperçu
            const previewWindow = window.open('', '_blank', 'width=800,height=600,scrollbars=yes');
            previewWindow.document.write(`
                <!DOCTYPE html>
                <html>
                <head>
                    <title>Aperçu - ${campaign.subject}</title>
                    <style>
                        body { font-family: Arial, sans-serif; padding: 20px; }
                        .preview-header { 
                            background: #f8f9fa; 
                            padding: 15px; 
                            border-radius: 8px; 
                            margin-bottom: 20px; 
                        }
                    </style>
                </head>
                <body>
                    <div class="preview-header">
                        <h2>${campaign.subject}</h2>
                        <p><strong>Date:</strong> ${new Date(campaign.createdAt).toLocaleDateString('fr-FR')}</p>
                        <p><strong>Statut:</strong> ${campaign.status}</p>
                    </div>
                    <div class="preview-content">
                        <p>Aperçu de la campagne disponible prochainement...</p>
                    </div>
                </body>
                </html>
            `);
        }

        // ==========================================
        // 📊 EXPORT DES DONNÉES
        // ==========================================
        async function exportHistory() {
            try {
                const response = await fetch('/admin/emails/export', {
                    method: 'GET'
                });

                if (response.ok) {
                    const blob = await response.blob();
                    const url = window.URL.createObjectURL(blob);
                    const a = document.createElement('a');
                    a.href = url;
                    a.download = `historique-emails-${new Date().toISOString().split('T')[0]}.csv`;
                    document.body.appendChild(a);
                    a.click();
                    document.body.removeChild(a);
                    window.URL.revokeObjectURL(url);
                } else {
                    alert('Erreur lors de l\'export');
                }
            } catch (error) {
                console.error('❌ Erreur export:', error);
                alert('Erreur lors de l\'export');
            }
        }

        // ==========================================
        // 📱 GESTION RESPONSIVE
        // ==========================================
        function handleResize() {
            const isMobile = window.innerWidth <= 768;
            const campaignsGrid = document.querySelector('.campaigns-grid');
            
            if (isMobile) {
                campaignsGrid.style.gap = '15px';
            } else {
                campaignsGrid.style.gap = '20px';
            }
        }

        window.addEventListener('resize', handleResize);
        handleResize(); // Appel initial
    </script>
</body>
</html>