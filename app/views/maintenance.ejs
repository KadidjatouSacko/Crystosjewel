<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Site en Maintenance - Crystos Jewel</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Roboto', sans-serif;
            background: linear-gradient(135deg, #b76e79 0%, #8a5465 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            overflow: hidden;
        }

        .maintenance-container {
            text-align: center;
            max-width: 600px;
            padding: 40px 20px;
            position: relative;
            z-index: 2;
            animation: fadeIn 0.8s ease-out;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .maintenance-icon {
            font-size: 80px;
            margin-bottom: 30px;
            opacity: 0.9;
            animation: pulse 2s infinite;
        }

        @keyframes pulse {
            0% { transform: scale(1); opacity: 0.9; }
            50% { transform: scale(1.1); opacity: 0.7; }
            100% { transform: scale(1); opacity: 0.9; }
        }

        .maintenance-title {
            font-size: 2.5rem;
            font-weight: 300;
            margin-bottom: 20px;
            letter-spacing: -0.5px;
        }

        .maintenance-message {
            font-size: 1.2rem;
            margin-bottom: 40px;
            opacity: 0.9;
            line-height: 1.6;
            background: rgba(255, 255, 255, 0.1);
            padding: 20px;
            border-radius: 15px;
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.2);
        }

        .countdown-container {
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
            border-radius: 15px;
            padding: 30px;
            margin-bottom: 40px;
            border: 1px solid rgba(255, 255, 255, 0.2);
        }

        .countdown-title {
            font-size: 1rem;
            margin-bottom: 20px;
            opacity: 0.8;
        }

        .countdown {
            display: flex;
            justify-content: center;
            gap: 20px;
            flex-wrap: wrap;
        }

        .countdown-item {
            background: rgba(255, 255, 255, 0.1);
            border-radius: 10px;
            padding: 15px 10px;
            min-width: 80px;
            border: 1px solid rgba(255, 255, 255, 0.1);
        }

        .countdown-number {
            font-size: 2rem;
            font-weight: bold;
            display: block;
        }

        .countdown-label {
            font-size: 0.8rem;
            opacity: 0.8;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        .social-links {
            margin-top: 40px;
        }

        .social-links a {
            color: white;
            text-decoration: none;
            margin: 0 15px;
            font-size: 1.5rem;
            opacity: 0.7;
            transition: all 0.3s ease;
        }

        .social-links a:hover {
            opacity: 1;
            transform: translateY(-2px);
        }

        .contact-info {
            margin-top: 30px;
            opacity: 0.8;
            font-size: 0.9rem;
            background: rgba(255, 255, 255, 0.05);
            padding: 15px;
            border-radius: 10px;
        }

        .status-indicator {
            position: fixed;
            top: 20px;
            right: 20px;
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
            padding: 10px 15px;
            border-radius: 25px;
            font-size: 0.8rem;
            border: 1px solid rgba(255, 255, 255, 0.2);
        }

        .status-dot {
            width: 8px;
            height: 8px;
            background: #ff6b6b;
            border-radius: 50%;
            display: inline-block;
            margin-right: 8px;
            animation: blink 1.5s infinite;
        }

        @keyframes blink {
            0%, 50% { opacity: 1; }
            51%, 100% { opacity: 0.3; }
        }

        .back-button {
            position: fixed;
            top: 20px;
            left: 20px;
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
            color: white;
            text-decoration: none;
            padding: 10px 15px;
            border-radius: 25px;
            font-size: 0.9rem;
            border: 1px solid rgba(255, 255, 255, 0.2);
            transition: all 0.3s ease;
        }

        .back-button:hover {
            background: rgba(255, 255, 255, 0.2);
            color: white;
            text-decoration: none;
        }

        /* Responsive */
        @media (max-width: 768px) {
            .maintenance-title {
                font-size: 2rem;
            }
            
            .maintenance-icon {
                font-size: 60px;
            }
            
            .countdown {
                gap: 10px;
            }
            
            .countdown-item {
                min-width: 60px;
                padding: 10px 5px;
            }
            
            .countdown-number {
                font-size: 1.5rem;
            }

            .status-indicator,
            .back-button {
                position: relative;
                top: auto;
                right: auto;
                left: auto;
                margin: 10px auto;
                display: block;
                text-align: center;
                width: fit-content;
            }
        }

        /* Animation de particules en arrière-plan */
        .particles {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            z-index: 1;
        }

        .particle {
            position: absolute;
            width: 4px;
            height: 4px;
            background: rgba(255, 255, 255, 0.3);
            border-radius: 50%;
            animation: float 15s infinite linear;
        }

        @keyframes float {
            0% {
                transform: translateY(100vh) rotate(0deg);
                opacity: 0;
            }
            10% {
                opacity: 1;
            }
            90% {
                opacity: 1;
            }
            100% {
                transform: translateY(-100vh) rotate(360deg);
                opacity: 0;
            }
        }

        .admin-info {
            position: fixed;
            bottom: 20px;
            left: 20px;
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
            padding: 10px 15px;
            border-radius: 10px;
            font-size: 0.8rem;
            border: 1px solid rgba(255, 255, 255, 0.2);
            max-width: 250px;
        }

        @media (max-width: 768px) {
            .admin-info {
                position: relative;
                bottom: auto;
                left: auto;
                margin: 20px auto 0;
                max-width: none;
            }
        }
    </style>
</head>
<body>
    <!-- Particules en arrière-plan -->
    <div class="particles" id="particles"></div>
    
    <!-- Indicateur de statut -->
    <div class="status-indicator">
        <span class="status-dot"></span>
        Maintenance en cours
    </div>

    <!-- Bouton retour pour les admins -->
    <a href="/admin/parametres" class="back-button">
        <i class="fas fa-arrow-left me-2"></i>
        Panneau Admin
    </a>

    <!-- Contenu principal -->
    <div class="maintenance-container">
        <div class="maintenance-icon">
            <i class="fas fa-tools"></i>
        </div>
        
        <h1 class="maintenance-title">Site en Maintenance</h1>
        
        <div class="maintenance-message">
            <i class="fas fa-info-circle me-2"></i>
            <%= message || "Nous effectuons actuellement une maintenance pour améliorer votre expérience. Merci de votre patience !" %>
        </div>

        <% if (scheduledEnd) { %>
        <div class="countdown-container">
            <div class="countdown-title">
                <i class="fas fa-clock me-2"></i>
                Retour estimé dans :
            </div>
            <div class="countdown" id="countdown">
                <div class="countdown-item">
                    <span class="countdown-number" id="hours">--</span>
                    <span class="countdown-label">Heures</span>
                </div>
                <div class="countdown-item">
                    <span class="countdown-number" id="minutes">--</span>
                    <span class="countdown-label">Minutes</span>
                </div>
                <div class="countdown-item">
                    <span class="countdown-number" id="seconds">--</span>
                    <span class="countdown-label">Secondes</span>
                </div>
            </div>
        </div>
        <% } %>

        <div class="social-links">
            <a href="#" title="Facebook" aria-label="Facebook">
                <i class="fab fa-facebook"></i>
            </a>
            <a href="#" title="Instagram" aria-label="Instagram">
                <i class="fab fa-instagram"></i>
            </a>
            <a href="#" title="Twitter" aria-label="Twitter">
                <i class="fab fa-twitter"></i>
            </a>
            <a href="mailto:contact@crystosjewel.com" title="Email" aria-label="Email">
                <i class="fas fa-envelope"></i>
            </a>
        </div>

        <div class="contact-info">
            <i class="fas fa-phone me-2"></i>
            <strong>Pour toute urgence :</strong><br>
            Email : contact@crystosjewel.com<br>
            Téléphone : +33 1 23 45 67 89
        </div>
    </div>

    <!-- Info pour les admins -->
    <div class="admin-info">
        <i class="fas fa-user-shield me-2"></i>
        <strong>Admin :</strong> Vous voyez cette page car la maintenance est active. 
        <a href="/admin/parametres" style="color: #fff; text-decoration: underline;">
            Gérer la maintenance
        </a>
    </div>

    <script>
        // Créer des particules animées
        function createParticles() {
            const particlesContainer = document.getElementById('particles');
            const particleCount = 30;

            for (let i = 0; i < particleCount; i++) {
                const particle = document.createElement('div');
                particle.className = 'particle';
                particle.style.left = Math.random() * 100 + '%';
                particle.style.animationDelay = Math.random() * 15 + 's';
                particle.style.animationDuration = (Math.random() * 10 + 10) + 's';
                particlesContainer.appendChild(particle);
            }
        }

        <% if (scheduledEnd) { %>
        // Démarrer le countdown si une date de fin est définie
        function updateCountdown() {
            try {
                const endTime = new Date('<%= scheduledEnd %>').getTime();
                const now = new Date().getTime();
                const distance = endTime - now;

                if (distance > 0) {
                    const hours = Math.floor((distance % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
                    const minutes = Math.floor((distance % (1000 * 60 * 60)) / (1000 * 60));
                    const seconds = Math.floor((distance % (1000 * 60)) / 1000);

                    document.getElementById('hours').textContent = hours.toString().padStart(2, '0');
                    document.getElementById('minutes').textContent = minutes.toString().padStart(2, '0');
                    document.getElementById('seconds').textContent = seconds.toString().padStart(2, '0');
                } else {
                    // Maintenance terminée, recharger la page
                    console.log('Maintenance terminée, rechargement...');
                    location.reload();
                }
            } catch (error) {
                console.error('Erreur countdown:', error);
            }
        }

        // Mettre à jour le countdown toutes les secondes
        updateCountdown();
        setInterval(updateCountdown, 1000);
        <% } %>

        // Vérifier périodiquement si la maintenance est terminée
        setInterval(() => {
            fetch('/api/test')
                .then(response => {
                    if (response.ok) {
                        console.log('Site accessible, rechargement...');
                        location.reload();
                    }
                })
                .catch(() => {
                    // Ignorer les erreurs, la maintenance est toujours active
                    console.log('Maintenance toujours active');
                });
        }, 30000); // Vérifier toutes les 30 secondes

        // Initialiser les particules
        createParticles();

        // Message de debug pour les développeurs
        console.log('🔧 Page de maintenance active');
        console.log('📅 Fin programmée:', '<%= scheduledEnd || "Non définie" %>');
        console.log('💬 Message:', '<%= message || "Message par défaut" %>');
    </script>
</body>
</html>